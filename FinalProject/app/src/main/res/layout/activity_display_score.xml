<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/blackboard"
    tools:context=".activity.DisplayScoreActivity">

    <TextView
        android:id="@+id/finalScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@android:color/white"
        android:textSize="@dimen/final_score_text_size" />

    <TextView
        android:id="@+id/scoreNotification"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/finalScore"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/score_notification_margin_bottom"
        android:text="@string/score_notification_text"
        android:textColor="@android:color/white"
        android:textSize="@dimen/score_notification_text_size" />

    <Button
        android:id="@+id/tryAgainButton"
        android:textColor="@color/colorAccent"
        android:background="@drawable/button_style"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/finalScore"
        android:layout_centerInParent="true"
        android:layout_marginTop="@dimen/try_again_button_margin_top"
        android:text="@string/try_again_text" />
</RelativeLayout>
